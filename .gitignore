node_modules/
.env
.env.local
.vercel

# Local secrets (Supabase DB password, etc.) — NEVER commit. Stays on this machine only.
secrets.local.md

# Legacy local file-store (no longer used; records now live in Supabase)
data/
public/uploads/

# NOTE: public/models and public/vendor are intentionally committed so the
# face-matching library and model weights deploy as static assets on Vercel.
